// Copy to src/Secrets.h for local development only
// NEVER commit Secrets.h - it's in .gitignore

// WiFi credentials are provisioned via CAN bus (ID 0x01) and stored in NVS.
// No hardcoded WiFi credentials needed.

// ESP-NOW broadcast address - REPLACE WITH YOUR GATEWAY MAC ADDRESS
// Format: {0xXX, 0xXX, 0xXX, 0xXX, 0xXX, 0xXX}
uint8_t broadcastAddress[] = {0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
